---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---

<time aria-label="Publish date" datetime={date.toISOString()}>
	{
		date.toLocaleDateString('en-us', {
			day: 'numeric',
			month: 'short',
			year: 'numeric',
		})
	}
</time>
