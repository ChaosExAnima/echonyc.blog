---
interface Props {
	caption?: string;
	src: string;
}

const { caption, src } = Astro.props;
---

<figure>
	<audio class="block w-full bg-violet-900/60" controls src={src}>
		<track
			default
			kind="captions"
			src={src.replace(/\.\w+$/, '.vtt')}
			srclang="en"
		/>
	</audio>
	{caption && <figcaption class="text-center text-sm">{caption}</figcaption>}
</figure>
