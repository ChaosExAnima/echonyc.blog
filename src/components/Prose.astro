---
import type { HTMLTag, Polymorphic } from 'astro/types';

type Props<Tag extends HTMLTag> = {
	class?: string;
} & Polymorphic<{ as: Tag }>;
const { as: Component = 'article', class: className } = Astro.props;
---

<Component
	class:list={[
		'prose prose-invert prose-violet sm:prose-xl prose-stone mx-auto',
		'prose-headings:font-thin hover:prose-a:text-violet-400 prose-a:transition-colors',
		'prose-h1:text-[length:var(--title-size)]',
		className,
	]}
>
	<slot />
</Component>
